# Ansible
Для работы у вас должен быть уже установлен Ansible и скопированы файлы из репозитория

### Задание 0
файл scriptik.sh - это выполненое 0 задание, команда чтобы запустить:
```bash
chmod +x scriptik.sh && ./scriptik.sh
```

###Задание 1 и 2
Для настройки Ansible нужно выполнить скрипт razvert.ssh и там же указать айпи адрес нужного сервера (также придется ввести пароль рута):
```bash
chmod +x razvert.ssh && ./razvert.ssh
```

После этого нужно запустить файлик yml с указанием порта (по умолчанию 80):
```bash
ansible-playbook autonginx.yml -e "nginx_port=9000"
```


